@import '../../../style/required'

.g-form-textarea
  font-weight 500
  font-size 15px
  line-height 18px
  color $basicOne
  display block
  outline 0
  padding 15px
  width 100%
  border-radius(4px)
  border 1px solid $basicFour
  background $white
  min-height 240px
  &.error
    border-color $red
  +placeholder()
    color $basicThree
