import buttonTheme from "./buttonTheme";
import buttonSize from "./buttonSize";
import buttonType from "./buttonType";
import color from "./color";

export default { buttonTheme, buttonSize, buttonType, color };
